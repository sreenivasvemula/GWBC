<?xml version="1.0"?>
<Entity
  name="Credit">
  <Columns>
    <Column
      beanPath="Credit.Account"
      name="Account"/>
    <Column
      beanPath="Credit.Amount"
      name="Amount"/>
    <Column
      beanPath="Credit.ReversedLink.DisplayName"
      name="Reversed"/>
  </Columns>
  <DisplayName><![CDATA[
gw.api.domain.accounting.ReversibleUtil.formatReversibleDisplayName(displaykey.DisplayName.Credit( Account, Amount?.render() ), Reversed)
  ]]></DisplayName>
</Entity>